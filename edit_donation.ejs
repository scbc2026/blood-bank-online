<!DOCTYPE html>
<html>
<head>
    <title>Edit Donation - Admin Only</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f0f2f5; padding: 20px; }
        .container { max-width: 800px; margin: auto; background: white; padding: 30px; border-radius: 10px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); }
        h2 { color: #d9534f; border-bottom: 2px solid #ddd; padding-bottom: 10px; }
        
        .row { display: flex; gap: 20px; margin-bottom: 15px; }
        .group { flex: 1; }
        label { display: block; font-weight: bold; margin-bottom: 5px; color: #555; }
        input, select { width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 5px; }
        
        .btn-update { width: 100%; padding: 15px; background: #f39c12; color: white; border: none; font-size: 18px; font-weight: bold; cursor: pointer; border-radius: 5px; margin-top: 20px; }
        .btn-update:hover { background: #e67e22; }
    </style>
</head>
<body>

    <div class="container">
        <h2>✏️ Edit Record (Admin Mode)</h2>
        <p style="color:red; font-size:14px;">Warning: You are modifying permanent records.</p>

        <form action="/update-donation/<%= donation._id %>" method="POST">
            
            <input type="hidden" name="donorId" value="<%= donor._id %>">

            <h3>👤 Personal Info</h3>
            <div class="row">
                <div class="group"><label>Name</label><input type="text" name="name" value="<%= donor.name %>"></div>
                <div class="group"><label>Father's Name</label><input type="text" name="fatherName" value="<%= donor.fatherName %>"></div>
            </div>
            <div class="row">
                <div class="group"><label>Age</label><input type="number" name="age" value="<%= donor.age %>"></div>
                <div class="group"><label>Mobile</label><input type="text" value="<%= donor.mobile %>" disabled style="background:#eee;"></div>
            </div>
            <div class="row">
                <div class="group">
                    <label>Blood Group</label>
                    <select name="bloodGroup">
                        <option value="<%= donor.bloodGroup %>"><%= donor.bloodGroup %></option>
                        <option value="A+">A+</option><option value="B+">B+</option><option value="O+">O+</option><option value="AB+">AB+</option>
                        <option value="A-">A-</option><option value="B-">B-</option><option value="O-">O-</option><option value="AB-">AB-</option>
                    </select>
                </div>
                <div class="group"><label>Address</label><input type="text" name="address" value="<%= donor.address %>"></div>
            </div>

            <hr>

            <h3>🩸 Donation Info</h3>
            <div class="row">
                <div class="group"><label>Bag Number</label><input type="text" name="bagNumber" value="<%= donation.bagNumber %>"></div>
                <div class="group">
                    <label>Donation Type</label>
                    <select name="donationType">
                        <option value="<%= donation.donationType %>"><%= donation.donationType %></option>
                        <option value="Voluntary">Voluntary</option>
                        <option value="Replacement">Replacement</option>
                    </select>
                </div>
            </div>

            <h4>🔬 Lab Results (Update if wrong)</h4>
            <div class="row">
                <div class="group"><label>HIV</label><select name="hiv"><option value="<%= donation.hiv %>"><%= donation.hiv %></option><option>Non-Reactive</option><option>Reactive</option></select></div>
                <div class="group"><label>HBsAg</label><select name="hbsag"><option value="<%= donation.hbsag %>"><%= donation.hbsag %></option><option>Non-Reactive</option><option>Reactive</option></select></div>
                <div class="group"><label>HCV</label><select name="hcv"><option value="<%= donation.hcv %>"><%= donation.hcv %></option><option>Non-Reactive</option><option>Reactive</option></select></div>
            </div>
            <div class="row">
                <div class="group"><label>Syphilis</label><select name="syphilis"><option value="<%= donation.syphilis %>"><%= donation.syphilis %></option><option>Non-Reactive</option><option>Reactive</option></select></div>
                <div class="group"><label>Malaria</label><select name="malaria"><option value="<%= donation.malaria %>"><%= donation.malaria %></option><option>Non-Reactive</option><option>Positive</option></select></div>
            </div>

            <button type="submit" class="btn-update">💾 Update Record</button>
            <br><br>
            <a href="/all-records" style="text-decoration:none; color:grey;">Cancel</a>
        </form>
    </div>

</body>
</html>